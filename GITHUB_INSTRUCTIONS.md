# 🚀 TitanHammer Pro - GitHub Build Instructions

## خطوات رفع المشروع على GitHub وبناء JAR تلقائياً

### الطريقة 1️⃣: من الجوال (GitHub Mobile App)

1. **حمّل تطبيق GitHub:**
   - [Android](https://play.google.com/store/apps/details?id=com.github.android)
   - [iOS](https://apps.apple.com/app/github/id1477376905)

2. **إنشاء Repository جديد:**
   ```
   - افتح التطبيق
   - اضغط + في الأعلى
   - New repository
   - الاسم: TitanHammerPro
   - Public ✅
   - Initialize with README ❌ (لا تفعّله)
   - Create repository
   ```

3. **رفع الملفات:**
   - افتح Repository
   - اضغط على "Add file"
   - "Upload files"
   - ارفع **كل** الملفات من المجلد (استخدم File Manager)
   - اكتب في Commit: "Initial commit"
   - اضغط "Commit changes"

4. **البناء التلقائي:**
   ```
   - انتظر 2-3 دقائق
   - اضغط على "Actions" في الأعلى
   - راح تشوف build يشتغل (نقطة صفراء 🟡)
   - لما يخلص يصير أخضر ✅
   ```

5. **تحميل الـ JAR:**
   ```
   - اضغط على الـ build الأخضر
   - انزل تحت لـ "Artifacts"
   - اضغط على "TitanHammerPro"
   - يحمّل لك ملف ZIP
   - فك الضغط وخذ الـ JAR منه!
   ```

---

### الطريقة 2️⃣: من المتصفح (على أي جهاز)

1. **روح [GitHub.com](https://github.com)**

2. **إنشاء Repository:**
   ```
   - اضغط على صورتك في الزاوية
   - Your repositories
   - New (الزر الأخضر)
   - Repository name: TitanHammerPro
   - Public
   - Create repository
   ```

3. **رفع الملفات:**
   ```
   - اضغط "uploading an existing file"
   - اسحب كل الملفات من المجلد
   - أو اضغط "choose your files" واختر كل شي
   - Commit: "Initial commit"
   - Commit changes
   ```

4. **تفعيل Actions (إذا طلب):**
   ```
   - اضغط على تبويب "Actions"
   - اضغط "I understand, enable them"
   ```

5. **راقب البناء:**
   ```
   - تبويب Actions
   - راح تشوف workflow يشتغل
   - انتظر لما يخضّر ✅
   - اضغط عليه
   - نزّل "TitanHammerPro" من Artifacts
   ```

---

### الطريقة 3️⃣: باستخدام Git CLI (متقدم)

```bash
# في مجلد المشروع
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/TitanHammerPro.git
git push -u origin main
```

---

## 📋 ملاحظات مهمة:

✅ **GitHub Actions مجاني** للـ public repositories
✅ **البناء تلقائي** عند كل push
✅ **الـ JAR متوفر** في Artifacts لمدة 90 يوم
✅ **ما تحتاج Maven** على جهازك!

## ⚡ كل مرة تعدل الكود:

1. ارفع الملفات الجديدة
2. انتظر البناء التلقائي
3. حمّل الـ JAR الجديد من Artifacts

## 🎯 الملفات المهمة المرفوعة:

```
TitanHammerPro/
├── .github/
│   └── workflows/
│       └── build.yml          ← سكريبت البناء التلقائي
├── src/                       ← السورس كود
├── pom.xml                    ← Maven config
├── README.md                  ← التوثيق
└── .gitignore                 ← الملفات المتجاهلة
```

## 🆘 مشاكل شائعة:

**❌ Build failed?**
- تأكد من رفع **جميع** الملفات
- تأكد من وجود pom.xml في الجذر

**❌ ما يطلع Artifacts?**
- انتظر البناء يخلص (يصير أخضر ✅)
- لازم يكون Repository public

**❌ Actions مو موجود?**
- تأكد من رفع مجلد .github/workflows/

---

## 🎉 بعد ما يخلص البناء:

1. روح لـ Actions tab
2. اضغط على آخر build أخضر ✅
3. انزل لـ Artifacts
4. حمّل "TitanHammerPro.zip"
5. فك الضغط
6. خذ TitanHammerPro-1.0.0.jar
7. حطه في مجلد plugins/
8. استمتع! 🎮

---

**الوقت الكلي:** 5-10 دقائق (معظمه انتظار البناء)
**سهولة:** ⭐⭐⭐⭐⭐
**مجاني:** ✅ 100%

© 2025 Blooddev - PrimeHost
